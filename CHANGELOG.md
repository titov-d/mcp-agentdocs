# Changelog

Notable changes to the agentdocs corpus and server. Dates are when documents
were verified against their primary sources (`YYYY-MM-DD`). The freshness badge
and "recent verifications" on the website are derived from this file and from
`corpus/manifest.json` — keep them in sync.

## 2026-06-11

### Added

- Claude API MCP connector documentation (topic `claude-mcp`): the Messages API
  MCP connector — beta header, server/toolset configuration, response blocks,
  limitations, OAuth, and remote third-party servers. Corpus now 8 documents.
- Initial corpus from the MCP specification 2025-11-25 (topic `mcp`): transports,
  tools, resources, prompts, lifecycle, authorization, and a TypeScript MCP
  server quickstart (7 documents).

All documents verified by hand against their primary sources on this date.
