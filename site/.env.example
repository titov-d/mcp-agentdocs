# Google Analytics 4 measurement id. When unset, no analytics is loaded.
NEXT_PUBLIC_GA_ID=

# Canonical site URL, used for Open Graph / canonical links. On Vercel this
# falls back to the deployment URL; locally it falls back to localhost.
NEXT_PUBLIC_SITE_URL=
